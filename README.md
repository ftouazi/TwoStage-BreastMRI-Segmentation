# UNet + YOLO Two-Stage Breast MRI Segmentation (DUKE / QIN)

This repository contains preprocessing, training and inference scripts for a two-stage pipeline: YOLO detection + segmentation (UNet++/UNet3+/AttentionUNet/TransUNet).
See `src/` for implementation and `src/preprocessing` for dataset conversion scripts.
